const fs=require('fs');const txt=fs.readFileSync('./src/views/cafeteriaData.ts','utf8');const blocks=[...txt.matchAll(/menus:\s*\[(.*?)\]/gs)];let idx=0;for(const b of blocks){const items=b[1].split('\n').map(l=>l.trim()).filter(l=>l.startsWith('{'));idx++;console.log('block'+idx,items.length);}const names=[...txt.matchAll(/name:\s*'([^']+)'/g)].map(m=>m[1]);console.log('total names',names.length);
